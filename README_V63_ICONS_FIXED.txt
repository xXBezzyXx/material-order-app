Version 63 - Icon Render Fix

I patched this uploaded version so image paths in app-data.json render as icons.

Changed:
- app.js
- admin.js
- style.css

If any icon still does not show, check ICON_PATHS_TO_CHECK.txt if present.
The filename and capitalization must match exactly.
