Version 35 - Submit Email Safe Fix

Built from the last working v32 app-data master version.

Fix:
- Submit Order uses the selected job's saved email.
- It does not reset your jobs/materials.

Important:
If you already exported your custom app-data.json, replace the app-data.json in this folder with YOUR exported app-data.json before uploading to Netlify.
